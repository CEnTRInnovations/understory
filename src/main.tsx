import React from 'react'
import ReactDOM from 'react-dom/client'
import ComplexityTimeline from './complexity_timeline'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ComplexityTimeline />
  </React.StrictMode>,
)
